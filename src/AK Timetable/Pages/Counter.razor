@page "/counter"

<h1>Counter</h1>

<p>Current count: @currentCount @_name</p>

<button class="btn btn-primary" @onclick="DoSomethingAsync">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
